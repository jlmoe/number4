#ifndef BONUS_POINTS_H
#define BONUS_POINTS_H

//function to determine if there exists a route between two nodes in an undirected graph
//int IsThereAPath(const int ** adjacency_matrix, const int num_nodes, const int source, const int destination);

//function that computes binary string from an unsigned integer
char * DecimalToBinaryString(const unsigned int decimal);

//function that identifies unique words in a string. It is case insensitive and words can be separated with multiple spaces
//char ** FindUniqueWords(const char * string, int * number of words found);

//function that counts the number of prime numbers between two numbers
//int CountPrimeNumbersInRange(const int int_1, const int int_2);

//function that finds a substring within a string
//int FindSubstring(const char * string, const char * substring);

//function that returns the number of integer pairs from an array that sum to specified number
//int CountPairsWithSum(const int * first, const int * last, const int * sumOfPair);

//AUX functions
void ReverseString(char * string);

#endif
